version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18
        - nvm use 18
        - yarn install
    artifacts:
    baseDirectory: .next  # ビルドアーティファクトが出力されるディレクトリ
    files:
      - '**/*'  # デプロイするファイルのパターン
