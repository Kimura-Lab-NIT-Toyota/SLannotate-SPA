version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18 #Nodeのバージョンを合わせるためにこのファイルを書いている
        - nvm use 18
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
  baseDirectory: .next  
  files:
    - '**/*' 
